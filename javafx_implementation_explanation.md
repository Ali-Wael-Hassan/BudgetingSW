# شرح تنفيذ صفحة الميزانية والأهداف (Budgets & Goals)

مرحباً! لقد قمت بتنفيذ التصميم الذي طلبته باستخدام JavaFX. 
كما طلبت، الصفحة تبدأ فارغة تماماً، ويمكنك إضافة الكروت (Cards) إليها برمجياً وقتما تشاء، وستظهر بنفس تصميم الصور التي أرسلتها.

إليك تفاصيل الملفات التي قمت بإنشائها أو التعديل عليها:

## 1. تصميم الصفحة الرئيسية (`goals.fxml`)
- **المسار:** `src/main/resources/com/duck/goals.fxml`
- **ماذا يفعل:** يحتوي على الهيكل الأساسي للصفحة (الخلفية الداكنة، العنوان "Budgets & Goals"، والأزرار "Filter" و "+ New Budget").
- **الجزء الأهم:** يحتوي على حاوية فارغة من نوع `FlowPane` (اسمها البرمجي `cardsContainer`). هذه الحاوية ستحمل الكروت لاحقاً، لكنها تبدأ فارغة كما طلبت.

## 2. تصميم الكارت الواحد (`goal_card.fxml`)
- **المسار:** `src/main/resources/com/duck/goal_card.fxml`
- **ماذا يفعل:** يمثل تصميم الكارت الواحد (مثل كارت "GROCERIES & DINING").
- يحتوي على النصوص (اسم الفئة، المبلغ المصروف، الحد الأقصى، نسبة الاستخدام، وعدد الأيام المتبقية).
- يحتوي على شريط التقدم (ProgressBar) والخط الملون على يسار الكارت.

## 3. ملف التنسيق (`styles.css`)
- **المسار:** `src/main/resources/com/duck/styles.css`
- **ماذا يفعل:** يعطي الألوان الداكنة والجمالية للتطبيق.
- قمت بتعريف ألوان مختلفة لشريط التقدم بناءً على النسبة: 
  - **أخضر (On Track):** إذا كان الاستهلاك أقل من 75%.
  - **أصفر (Approaching Limit):** إذا كان الاستهلاك بين 75% و 90%.
  - **أحمر (Action Required):** إذا كان الاستهلاك تخطى 90%.

## 4. متحكم الصفحة الرئيسية (`GoalsController.java`)
- **المسار:** `src/main/java/com/duck/GoalsController.java`
- **ماذا يفعل:** يتحكم في الصفحة الرئيسية.
- يحتوي على دالة مهمة جداً وهي `addGoalCard(...)`.
- **كيف تستخدمه؟**
  عندما تريد إضافة كارت جديد للصفحة من الكود الخاص بك (مثلاً بعد جلب البيانات من قاعدة البيانات)، يمكنك ببساطة استدعاء هذه الدالة وتمرير البيانات لها هكذا:
  ```java
  addGoalCard("GROCERIES & DINING", 850, 1000, 12);
  ```
  وستقوم الدالة أوتوماتيكياً برسم الكارت وإضافته للصفحة.

## 5. متحكم الكارت الواحد (`GoalCardController.java`)
- **المسار:** `src/main/java/com/duck/GoalCardController.java`
- **ماذا يفعل:** يأخذ البيانات التي مررتها (المصروف، والحد الأقصى) ويقوم بحساب النسبة المئوية أوتوماتيكياً (`percentage`).
- بناءً على النسبة، يقوم بتغيير لون شريط التقدم والخط الجانبي (إلى الأخضر، الأصفر، أو الأحمر).

---
**جاهز للاستخدام!** الآن يمكنك تحميل `goals.fxml` في تطبيقك، وستجد صفحة مطابقة للتصميم، فارغة وتنتظر إضافة الكروت إليها برمجياً باستخدام `GoalsController`.
